import 'package:flutter/material.dart';

class SliderIcon {
  bool isSelected;
  final String title;
  final String image;

  SliderIcon({@required this.image, @required this.title, this.isSelected});
}
